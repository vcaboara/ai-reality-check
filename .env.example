# Environment variables for AI Reality Check

# AI Provider Configuration
# Option 1: Gemini API (cloud, fast, requires API key)
GEMINI_API_KEY=your_gemini_api_key_here

# Option 2: Ollama (local, private, no API key needed)
# Recommended models for technical analysis:
#   - qwen2.5-coder:7b (balanced, good quality)
#   - qwen2.5-coder:14b (better quality, slower)
#   - qwen2.5-coder:32b (best quality, requires 32GB+ RAM)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen2.5-coder:7b

# Flask Configuration
FLASK_ENV=development
PORT=5000
# SECRET_KEY=your-random-secret-key-here  # Optional: Flask session encryption (auto-generates if not set)

# Domain Configuration (optional - defaults to config/domain.yaml)
# DOMAIN_CONFIG_PATH=config/domain.yaml
