[AI] fix: render results page as HTML, add config-based field defaults

Fixes:
- /results now renders results.html template (not JSON)
- Created /api/results endpoint for AJAX calls
- results.html fetches from /api/results

New feature - Config-based defaults:
- Created config/user_preferences.yaml for default values
- Fields use: localStorage > config defaults > blank
- Useful for teams with common project parameters

Priority: localStorage (user input) > config file > empty

Example user_preferences.yaml:
```yaml
project:
  default_title: "Thermal Processing System"
  default_context: "Budget: $500K\nTimeline: 18 months"
```

---
AI-Generated-By: GitHub Copilot (Claude Sonnet 4.5)
